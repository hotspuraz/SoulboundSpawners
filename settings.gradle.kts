plugins {
    // Lets Gradle download a matching JDK if the build machine doesn't have one.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "SoulboundSpawners"
